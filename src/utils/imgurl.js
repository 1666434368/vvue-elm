export function imgUrl750 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.png/?imageMogr/format/webp/thumbnail/750x/'
  return imgUrl
}

export function imgUrl150 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.png/?imageMogr/format/webp/thumbnail/150x/'
  return imgUrl
}

export function imgUrl686 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.png/?imageMogr/format/webp/thumbnail/686x/'
  return imgUrl
}

export function imgUrl26 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.png/?imageMogr/format/webp/thumbnail/26x/'
  return imgUrl
}

export function imgUrl240 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.jpeg/?imageMogr/format/webp/thumbnail/240x/'
  return imgUrl
}

export function imgUrl140 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.jpeg/?imageMogr/format/webp/thumbnail/140x/'
  return imgUrl
}

export function imgUrl60 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.jpeg/?imageMogr/format/webp/thumbnail/!60x60r/gravity/Center/crop/60x60/'
  return imgUrl
}

export function imgUrl300 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.jpeg/?imageMogr/format/webp/thumbnail/300x/'
  return imgUrl
}

export function imgUrl460 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.png/?imageMogr/format/webp/thumbnail/!460x260r/gravity/Center/crop/460x260/'
  return imgUrl
}

export function imgUrl200 (path) {
  let imgUrl = 'https://fuss10.elemecdn.com/' + path.substring(0, 1) + '/' + path.substring(1, 3) + '/' + path.substring(3) + '.jpeg/?imageMogr/format/webp/thumbnail/!200x200r/gravity/Center/crop/200x200/'
  return imgUrl
}
